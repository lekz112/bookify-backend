schema: schema.graphql
overwrite: true
generates:
  src/types.ts:    
    config:
      contextType: ./createContextFunction#BookifyContext
    plugins:      
      - time
      - typescript-common
      - typescript-server
      - typescript-resolvers