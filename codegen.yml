schema: schema.graphql
overwrite: true
generates:
  src/types.ts:    
    config:
      contextType: ./index#BookifyContext
    plugins:      
      - time
      - typescript-common
      - typescript-server
      - typescript-resolvers